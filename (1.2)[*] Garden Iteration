/*
Iterates through each cell in the garden and has it run some function.
*/

gardenIteration: function(func) {
  for (var j = 0; j < eden.y; j++) {
    for (var i = 0; i < eden.x; i++) {
      func(eden.garden[j][i]);
    }
  }
};
